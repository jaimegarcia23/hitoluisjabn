<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hitoluisja">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hitoluisja"
        tools:targetApi="31">

        <!-- Declaración de MainActivity: actividad inicial -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de CharacterSelectionActivity: para seleccionar el personaje -->
        <activity
            android:name=".CharacterSelectionActivity"
            android:exported="true" />

        <!-- Declaración de GameActivity: para el juego -->
        <activity
            android:name=".GameActivity"
            android:exported="true" />

        <!-- Declaración de RankingActivity: para mostrar el ranking (si la tienes) -->
        <activity
            android:name=".RankingActivity"
            android:exported="true" />

    </application>

</manifest>
